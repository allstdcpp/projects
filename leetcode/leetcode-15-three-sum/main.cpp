#include <iostream>
#include <unordered_map>
#include <vector>

class Solution
{
public:

    std::vector<std::vector<int>> twoSum(std::vector<int>& n)
    {
        std::vector<std::vector<int>> result;

        return result;
    }
};

int main(int argc, char** argv) { return 0; }
